<!-- 
============================================
Title: appointment-list.ejs
Author: Erin Brady
Date: 24 February 2023
Description: Express.js Output for Pets-R-Us Appointment List Page
============================================
-->

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400;1,500;1,600&display=swap" rel="stylesheet">

    <!-- Custom Styles -->
    <link rel="stylesheet" href="/styles/site.css">
    <link rel="stylesheet" href="/styles/customer-list.css">
</head>
<body>
    <!-- Navigation Bar -->
    <%- include('./partials/_header.ejs') %>

    <!-- Appointments Page Main Content -->
    <main>
        <h2>My Appointments</h2>
        <% if(appointments.length > 0) { %>
            <table>
                <tr>
                    <th>First Name</th>
                    <th>Last Name</th>
                    <th>Email</th>
                    <th>Service</th>
                </tr>
                <tbody>
                    <% appointments.forEach(appointment => { %>
                        <tr>
                            <td>
                                <%= appointment.firstName %>
                            </td>
                            <td>
                                <%= appointment.lastName %>
                            </td>
                            <td>
                                <%= appointment.email %>
                            </td>
                            <td>
                                <%= appointment.service %>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
            <% } else { %>
                <h3>No appointments have been added to the MongoDB database. Try booking one!</h3>
            <% } %>
    </main>

    <!-- Footer -->
    <%- include('./partials/_footer.ejs') %>
</body>
</html>
